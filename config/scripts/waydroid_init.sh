#! /usr/bin/bash

sudo waydroid init -c https://ota.waydro.id/system -v https://ota.waydro.id/vendor #the GUI-initialization didn't work when I tried it, due to missing sudo rights. Execute it here automatically to run Android apps.
